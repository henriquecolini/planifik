name: Deploy
on:
  release:
    types: [ published ]
jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker BuildX
        uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/planifik:${{ github.event.release.tag_name }}
            ghcr.io/${{ github.repository_owner }}/planifik:latest
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/planifik:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/planifik:cache,mode=max